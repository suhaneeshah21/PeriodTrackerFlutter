# TODO

- [ ] Remove the `_isPeriodDay` method from `lib/screens/cycle_log_screen.dart`.
- [ ] Remove `isPeriod` variable and all references to `isPeriod` in the horizontal strip `itemBuilder`.
- [ ] Update the calendar date circle styling to no longer depend on `periodStartDate` highlighting.
- [ ] Run `flutter analyze` (or at least `flutter test`/`flutter build` if configured) to confirm no compile errors.

